version: 2

models:
    - name: _store__store_sales_dbt_model
      description: "A starter store model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null